import { styled } from "styled-components";

export const DialogContainer = styled.div`
    dialog {
        border-radius: 20px;
    }
    .close-btn {
        position: fixed;
        padding: 10px;
        margin-left: 190px;
        margin-top: -10px;
    }
`;